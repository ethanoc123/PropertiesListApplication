import { Property } from "../../types/types";

interface PropertyListingCardProps {
  property: Property;
}

export default function PropertyCard({ property }: PropertyListingCardProps) {
  return <div></div>;
}
